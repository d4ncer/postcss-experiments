# PostCSS experiments

Playground to test `postcss` plugins & transformations.

# Usage

Clone or fork (recommended) this repo and add plugins to `postcss.config.js`.
Output is by default to `output.css`, but this can be changed in `index.js`.

```sh
$ node index.js
$ cat output.css
```
